<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thesis.mapper.NoticeMapper">

    <select id="selectById" resultType="map">
        SELECT * FROM sys_notice WHERE id = #{id}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM sys_notice
    </select>

    <select id="selectAllOrderByIdDesc" resultType="map">
        SELECT * FROM sys_notice ORDER BY id DESC
    </select>

    <select id="selectAllOrderByPinnedDesc" resultType="map">
        SELECT * FROM sys_notice ORDER BY pinned DESC, id DESC
    </select>

    <select id="selectApprovedOrderByIdDesc" resultType="map">
        SELECT * FROM sys_notice
        WHERE (audit_status='approved' OR audit_status IS NULL OR audit_status='')
        ORDER BY id DESC
    </select>

    <select id="selectApprovedOrderByPinnedDesc" resultType="map">
        SELECT * FROM sys_notice
        WHERE (audit_status='approved' OR audit_status IS NULL OR audit_status='')
        ORDER BY pinned DESC, id DESC
    </select>

    <select id="selectBySubmitterOrderByIdDesc" resultType="map">
        SELECT * FROM sys_notice
        WHERE submitter_username = #{submitter}
        ORDER BY id DESC
    </select>

    <select id="selectBySubmitterOrderByPinnedDesc" resultType="map">
        SELECT * FROM sys_notice
        WHERE submitter_username = #{submitter}
        ORDER BY pinned DESC, id DESC
    </select>
</mapper>
