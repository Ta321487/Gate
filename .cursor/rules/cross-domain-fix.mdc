---
description: 选题只是探针；修 bug 先判断是否通用（大概率是），扫同类路径同修，禁止单题特例糊弄
alwaysApply: true
---

# 选题探针 → 跨域同修

日常修 bug / 换皮 / 文案 / Store 行为时适用。不替代正式交付审计轮（见 `delivery-audit.mdc`）。

## 硬原则

1. **探针，不是单题工厂**：某个开题把问题捅开，不等于要为该题长期打补丁。改工厂要让换开题也不再中招。
2. **自行判断通用性（不默认）**：选题暴露 ≠ 自动定性为工厂通用缺陷。先看缺陷落在共享代码还是域专属配置；经验上大概率通用，但须用搜索/对照证据收口，再决定修面。
3. **动手前必扫同类位点**：定位 bug 路径后立刻搜是否别处同病，例如：
   - 同能力其它域皮肤（themes / SQL templates / builders presets）
   - 同文案键其它 labels / seeds
   - jdbc / mybatis / jpa 三套 Store 若逻辑同源
   - baseline + overlays 镜像文件
4. **修法优先级**：
   - 确认通用 → 共享机制（清洗/禁词/门禁/基线）> 批量改同类皮肤；禁止只改当前选题包。
   - 确认独有 → 只改该域，并写清「为何其它域不会中招」。
5. **收尾汇报**：简短交代判断结论（通用/独有）、扫了哪些面、一并改了什么——不写成长审计。

## 禁止

- 只改当前选题/当前包的特例，而不扫同类路径。
- 把「每修一处全厂交付审计」当成日常流程（过重）；日常是判断 + 同类路径同修。
- 用单域硬编码文案/逻辑冒充已修好工厂通用问题。
