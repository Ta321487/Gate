---
description: 工厂主链路：材料→匹配 DOM-*+能力岛→bake 可答辩 ZIP；一切改动对照此链
alwaysApply: true
---

# 工厂核心

## 主链路（一切工作的中心）

**开题/材料 → 匹配到 `DOM-*` + 能力岛 → bake 出可答辩 ZIP**

出包必须跟材料一致，且：

**实包 ≈ README ≈ 论文「系统实现」**

| 阶段 | 干什么 | 成功长什么样 |
|------|--------|----------------|
| 材料 | 认出要做哪类业务系统 | 不改开题迁就工厂 |
| 匹配 | 落到具名域 + 能力/开关 | 不抢邻域；不能交则双显/拒收，禁止冒充 |
| bake | 生成可演示主路径的 ZIP | 有表/API/状态机/菜单/种子；不是空壳 |

日常修 bug、换皮、补列、加开关，都是在**加固这条链**，不是另起一套产品。

## 改工厂时怎么站队

1. 选题只是**探针**：修共享机制 / 域皮 / bake，让换开题也不再中招（见 `cross-domain-fix`）。
2. 能力或开关开了 → SQL 有列、菜单有入口、三套 Store 对齐；缺列先补 bake，硬失败只是保险丝。
3. 翻车可以驱动，但本次要把同坑写成 `ensure_*` / 门禁 / 测试，禁止只擦当面学生包。

## 绝对禁止

- 改开题正文/样例迁就模板。
- 宣称已交却缺表/API/规则/状态机（空壳）。
- 只改当前选题包 / 单套 Store。
- 工厂 `app/llm` 或运营 Key 进学生 ZIP。
- 假成功：用户填了 / 能力开了，写库失败或缺列仍返回成功。
- 新开厚 DOM 包；把场景 / 技术栈 / AI 三轴揉乱。

## 改完必汇报（短）

- 动的是主链路哪一段（匹配 / bake / 骨架 / 域皮）？
- 通用还是独有？扫了哪些面？
- 出包是否仍能答辩演示？有没有只把错抛给学生包？

长表：`HANDOFF.md` · `docs/delivery-audit-rules.md` · `docs/capabilities.md` · `docs/domains.md`
