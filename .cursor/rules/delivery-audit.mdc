---
description: 交付审计与域/能力落地必须以 docs/delivery-audit-rules.md 为依据
alwaysApply: true
---

# 交付审计依据（强制）

审交付、标已齐、落地新域/新能力、或声称换皮全覆盖进度时：

1. **必须遵循** [`docs/delivery-audit-rules.md`](docs/delivery-audit-rules.md)（工厂主链路：材料→匹配→出包；红灯+老板小卡）。
2. 审计报告须有：**总表、红灯、旧题回归、老板小卡 12 条**；缺小卡 → 无效。
3. 审计轮 **默认只出报告、不改代码**（用户明确下令才改）。
4. 待办 ID 见 [`docs/domain-skin-gap-analysis.md`](docs/domain-skin-gap-analysis.md)；伤旧或落不准不得标已齐。
5. 场景轴见 `opening-scene.mdc`；技术栈轴见 `tech-stack-delivery.mdc`——两轴勿混。
6. **常见好做默认实现**（见 `delivery-audit-rules.md` §1.1 / H23）：有时间·布局·额度等字段就挂简单规则（过期下架、分栏、拒过截止等）；**不要**刻意收窄。材料未写且非主路径必需（真支付/地图/人脸等）→ 可不做，勿与「省过头」混为一谈。
7. **组审进度**：借用/占用组已收口（2026-08-04）；当前可开 **报修/工单组**。进度以清单册与 `delivery-audit-rules.md` §3.4 为准。
